# Codeness repository instructions

- Before declaring implementation work complete, run `./scripts/build-quiet.sh`. A successful completion build must install the signed Release app at `/Applications/Codeness.app`, not stop at a DerivedData product.
